const ItemList = () => {

    // Solo el map
    

    return (
<div>
    <h2>El auto</h2>
    {/* {map((e)=>{
        return <p key={e.id}>{e.marca} {e.modelo}</p>
    })} */}
</div>
    )
}

export default ItemList
